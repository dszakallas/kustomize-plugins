apiVersion: example.com/v1
kind: CustomResource
metadata:
  name: example
  annotations:
    config.kubernetes.io/local-config: "true"
spec:
  some:
    nested: value
  other:
    things:
      - list
      - of
      - values
